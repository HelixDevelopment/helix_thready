// GENERATED — DO NOT EDIT.
// Generated by tokens-bridge/generate.mjs from ../opendesign/tokens.css
// sha256(tokens.css) = f0f788d1ee73dc624b2006b597d6fb9ab618190b68bf2425c72edc36f0ca4eaf
// Single source of truth: opendesign/tokens.css (design-system.md §3/§7).
// Not exported (CSS-runtime color-mix()/var() formulas): --accent-hover, --accent-active, --elev-flat, --elev-ring, --elev-raised, --focus-ring.
// Contract for UI-Components-KMP [GAP: 8.4] — replaces the hand-kept foreign palette.
// Package: design-system.md §7 sample declares no package — digital.vasic.thready.design [DEFAULT — adjustable].

package digital.vasic.thready.design

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.em
import androidx.compose.ui.unit.sp

const val THREADY_THEME_ID = "thready" // --theme-id

object ThreadyColors {
    object LightColors {
        val Brand = Color(0xFFB6E376) // --brand (light)
        val Brand2 = Color(0xFFABDDC9) // --brand-2 (light)
        val BrandInk = Color(0xFF0A0F04) // --brand-ink (light)
        val Bg = Color(0xFFFFFFFF) // --bg (light)
        val Surface = Color(0xFFFFFFFF) // --surface (light)
        val SurfaceWarm = Color(0xFFF1F5F9) // --surface-warm (light)
        val Fg = Color(0xFF020817) // --fg (light)
        val Muted = Color(0xFF475569) // --muted (light)
        val Border = Color(0xFFE2E8F0) // --border (light)
        val BorderStrong = Color(0xFF64748B) // --border-strong (light)
        val Accent = Color(0xFF446E12) // --accent (light)
        val AccentOn = Color(0xFFFFFFFF) // --accent-on (light)
        val Success = Color(0xFF166534) // --success (light)
        val Warn = Color(0xFF854D0E) // --warn (light)
        val Danger = Color(0xFFDC2626) // --danger (light)
        val Fg2 = Fg // --fg-2 → var(--fg)
        val Meta = Muted // --meta → var(--muted)
        val BorderSoft = Border // --border-soft → var(--border)
        val DsHeart = Accent // --ds-heart → var(--accent)
    }

    object DarkColors {
        val Brand = Color(0xFFB6E376) // --brand (dark)
        val Brand2 = Color(0xFFB7EBD6) // --brand-2 (dark)
        val BrandInk = Color(0xFF0A0F04) // --brand-ink (dark)
        val Bg = Color(0xFF020817) // --bg (dark)
        val Surface = Color(0xFF020817) // --surface (dark)
        val SurfaceWarm = Color(0xFF1E293B) // --surface-warm (dark)
        val Fg = Color(0xFFF8FAFC) // --fg (dark)
        val Muted = Color(0xFF94A3B8) // --muted (dark)
        val Border = Color(0xFF1E293B) // --border (dark)
        val BorderStrong = Color(0xFF64748B) // --border-strong (dark)
        val Accent = Color(0xFFB6E376) // --accent (dark)
        val AccentOn = Color(0xFF0A0F04) // --accent-on (dark)
        val Success = Color(0xFF16A34A) // --success (dark)
        val Warn = Color(0xFFEAB308) // --warn (dark)
        val Danger = Color(0xFFEF4444) // --danger (dark)
        val Fg2 = Fg // --fg-2 → var(--fg)
        val Meta = Muted // --meta → var(--muted)
        val BorderSoft = Border // --border-soft → var(--border)
        val DsHeart = Accent // --ds-heart → var(--accent)
    }
}

object ThreadySpacing {
    val s1 = 4.dp // --space-1
    val s2 = 8.dp // --space-2
    val s3 = 12.dp // --space-3
    val s4 = 16.dp // --space-4
    val s5 = 20.dp // --space-5
    val s6 = 24.dp // --space-6
    val s8 = 32.dp // --space-8
    val s12 = 48.dp // --space-12
    val sectionYDesktop = 80.dp // --section-y-desktop
    val sectionYTablet = 48.dp // --section-y-tablet
    val sectionYPhone = 32.dp // --section-y-phone
    val containerMax = 1200.dp // --container-max
    val containerGutterDesktop = 24.dp // --container-gutter-desktop
    val containerGutterTablet = 16.dp // --container-gutter-tablet
    val containerGutterPhone = 12.dp // --container-gutter-phone
}

object ThreadyRadius {
    val sm = 8.dp // --radius-sm
    val md = 12.dp // --radius-md
    val lg = 16.dp // --radius-lg
    val pill = 9999.dp // --radius-pill
}

object ThreadyTypeScale {
    val textXs = 12.sp // --text-xs
    val textSm = 14.sp // --text-sm
    val textBase = 16.sp // --text-base
    val textLg = 20.sp // --text-lg
    val textXl = 24.sp // --text-xl
    val text2xl = 32.sp // --text-2xl
    val text3xl = 48.sp // --text-3xl
    val text4xl = 64.sp // --text-4xl
    val leadingBody = 1.5f // --leading-body (line-height multiplier)
    val leadingTight = 1.2f // --leading-tight (line-height multiplier)
    val trackingDisplay = (-0.01).em // --tracking-display
}

object ThreadyMotion {
    const val FAST_MILLIS = 150 // --motion-fast
    const val BASE_MILLIS = 200 // --motion-base
}
