# =============================================================================
#  Helix Thready — buf module config (helix_proto pattern)
#  Location : docs/public/research/mvp/api/materials/codegen/buf.yaml
#  Provenance: [VERIFIED: helix_proto/buf.yaml] modules:[proto]; lint STANDARD;
#              breaking: use:[FILE] (sdk-strategy.md §3). Template scaffold.
# =============================================================================
version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
